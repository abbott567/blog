---
eleventyExcludeFromCollections: true
title: 'Orphaned input demo - HTML: forms'
meta:
  description: 'An example of an orphaned input and how it impacts accessibility.'
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">

    <title>{{title}} - {{site.name}}</title>
    {%- if meta.canonicalURL -%}
      <link rel="canonical" href="{{meta.canonicalURL}}" />
    {%- else -%}
      <link rel="canonical" href="https://craigabbott.co.uk{{page.url}}" />
    {%- endif -%}

    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">

    <meta name="title" content="{{title}}">
    <meta name="description" content="{{meta.description}}">

    {%- include 'meta/theme.njk' -%}
    {%- include 'meta/card-twitter.njk' -%}
    {%- include 'meta/card-og.njk' -%}

    <link rel="icon" href="data:image/svg+xml,%3Csvg%20xmlns='http://www.w3.org/2000/svg'%20viewBox='0%200%20100%20100'%3E%3Ctext%20y='.9em'%20font-size='90'%3E😸%3C/text%3E%3C/svg%3E">
    <style>
      html { font-family:sans-serif; font-size:1.5rem; padding:5%; }
      label { display:block; }
      input { font-size: 1rem; border: 1px solid black; padding:10px; margin-bottom:1rem; }
      input:focus { border: 5px solid black; outline: 3px solid gold; }
    </style>
  </head>

  <body>
    <main>
      <h1>Orphaned input</h1>

      <p>In this example, the input for last name is orphaned.</p>

      <form>
        <label for="first-name">First name</label>
        <input type="text" id="first-name">

        <label>Last name</label>
        <input type="text">
      </form>

    </main>

    <script>
      const form = document.querySelector('form')
      form.addEventListener('submit', e => e.preventDefault())
    </script>
  </body>
</html>